{
  "name": "accessibility-demos",
  "version": "1.0.0",
  "description": "A complete WCAG 2.2 teaching suite: a runnable failing and passing demo for every success criterion at levels A, AA and AAA, plus a full WCAG-EM evaluation walkthrough.",
  "type": "module",
  "private": true,
  "license": "CC-BY-4.0",
  "scripts": {
    "dev": "npx --yes serve -l 3000 .",
    "test:a11y": "node tools/run-axe.mjs",
    "test:pa11y": "npx --yes pa11y-ci --config tools/.pa11yci.json",
    "test:links": "node tools/check-links.mjs",
    "test:data": "node tools/verify-criteria.mjs",
    "test": "npm run test:data && npm run test:contrast && npm run test:links && npm run test:reflow && npm run test:a11y",
    "build": "node tools/build.mjs",
    "test:contrast": "node tools/check-contrast.mjs",
    "test:reflow": "node tools/check-reflow.mjs"
  },
  "devDependencies": {
    "@axe-core/puppeteer": "^4.10.0",
    "axe-core": "^4.10.0",
    "puppeteer": "^23.0.0",
    "pa11y-ci": "^3.1.0",
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
